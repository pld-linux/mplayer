--- MPlayer-20020320/mplayer.c.wiget	Tue Mar 19 20:22:00 2002
+++ MPlayer-20020320/mplayer.c	Wed Mar 20 14:57:34 2002
@@ -398,7 +398,7 @@
 {
 char *conffile;
 int conffile_fd;
-if (m_config_parse_config_file(conf, "/etc/mplayer.conf") < 0)
+if (m_config_parse_config_file(conf, "/etc/mplayer/mplayer.conf") < 0)
   exit(1);
 if ((conffile = get_path("")) == NULL) {
   mp_msg(MSGT_CPLAYER,MSGL_WARN,MSGTR_NoHomeDir);
@@ -605,10 +605,12 @@
 
 // check codec.conf
 if(!parse_codec_cfg(get_path("codecs.conf"))){
+  if(!parse_codec_cfg("/etc/mplayer/codecs.conf")){
   if(!parse_codec_cfg(CONFDIR"/codecs.conf")){
     mp_msg(MSGT_CPLAYER,MSGL_HINT,MSGTR_CopyCodecsConf);
     exit(0);  // From unknown reason a hangup occurs here :((((((
   }
+  }
 }
 
     if(audio_codec && strcmp(audio_codec,"help")==0){
