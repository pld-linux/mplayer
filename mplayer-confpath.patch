diff -urN MPlayer-20010902/mplayer.c MPlayer-20010902.new/mplayer.c
--- MPlayer-20010902/mplayer.c	Sat Sep  1 21:43:41 2001
+++ MPlayer-20010902.new/mplayer.c	Wed Sep  5 14:05:36 2001
@@ -364,7 +364,7 @@
 {
 char *conffile;
 int conffile_fd;
-if (parse_config_file(conf, "/etc/mplayer.conf") < 0)
+if (parse_config_file(conf, "/etc/mplayer/mplayer.conf") < 0)
   exit(1);
 if ((conffile = get_path("")) == NULL) {
   mp_msg(MSGT_CPLAYER,MSGL_WARN,MSGTR_NoHomeDir);
@@ -508,10 +508,12 @@
 
 // check codec.conf
 if(!parse_codec_cfg(get_path("codecs.conf"))){
-  if(!parse_codec_cfg(DATADIR"/codecs.conf")){
-    mp_msg(MSGT_CPLAYER,MSGL_HINT,MSGTR_CopyCodecsConf);
-    exit(1);
-  }
+  if(!parse_codec_cfg("/etc/mplayer/codecs.conf")){
+    if(!parse_codec_cfg(DATADIR"/codecs.conf")){
+	mp_msg(MSGT_CPLAYER,MSGL_HINT,MSGTR_CopyCodecsConf);
+	exit(1);
+    }
+  }    
 }
 
 // check font
